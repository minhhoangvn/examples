- name: "Clean runtime files"
  file:
    path: "{{ playbook_dir }}/../../templates/{{ item }}.yaml"
    state: absent
  with_items:
        - proxy-config
        - credentials
        - istio-config
  changed_when: false